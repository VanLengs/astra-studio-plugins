---
description: Create a domain canvas mapping business domains, boundaries, and relationships
argument-hint: [domain description or workspace path]
---

Map the business domains for `$ARGUMENTS`. Classifies core/supporting/generic, draws boundaries, and maps relationships between domains.

Use skill: "domain-canvas"
