---
description: Create a behavior matrix cross-referencing actors, actions, events, and data
argument-hint: [domain description or workspace path]
---

Build a behavior matrix for `$ARGUMENTS`. Cross-references every actor, action, event, and data flow to reveal gaps, redundancies, and automation opportunities.

Use skill: "behavior-matrix"
